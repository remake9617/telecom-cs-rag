# =====================================================================
# 环境变量模板（复制为 .env 后填入真实值；.env 已被 .gitignore 忽略，绝不入库 D19）
# 用法：cp .env.example .env  然后编辑 .env
# =====================================================================

# ---------- 中间件（docker-compose.yml 读取）----------
MYSQL_ROOT_PASSWORD=cs_root_123
MYSQL_DATABASE=cs_db

# ---------- 模型 API Key（后端 application.yml 读取）----------
# 阿里百炼 DashScope（Chat 主力 qwen-plus）：https://bailian.console.aliyun.com/
AI_DASHSCOPE_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 硅基流动（Embedding bge-m3 / Rerank bge-reranker-v2-m3）：https://cloud.siliconflow.cn/
SILICONFLOW_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
SILICONFLOW_BASE_URL=https://api.siliconflow.cn

# ---------- 中间件连接地址（后端连 VM 时填 VM 的 IP）----------
# 若后端与中间件同在 VM 内，用 localhost；若后端在 Windows、中间件在 VM，填 VM IP
ES_URIS=http://localhost:9200
