<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.cs</groupId>
        <artifactId>cs-parent</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>cs-ingestion</artifactId>
    <name>cs-ingestion</name>
    <description>入库层：文档解析 → 分块 → Embedding → ES 索引；顺序入库流水线（MVP）</description>

    <dependencies>
        <dependency>
            <groupId>com.cs</groupId>
            <artifactId>cs-framework</artifactId>
        </dependency>
        <dependency>
            <groupId>com.cs</groupId>
            <artifactId>cs-infra-ai</artifactId>
        </dependency>
        <!-- 入库需写文档/chunk 元数据（cs-knowledge 的 entity/mapper）+ 复用其传递的 ES client -->
        <dependency>
            <groupId>com.cs</groupId>
            <artifactId>cs-knowledge</artifactId>
        </dependency>
        <!-- 文档解析（M1）：Tika 统一解析 PDF/Word/Excel/MD/TXT -->
        <dependency>
            <groupId>org.apache.tika</groupId>
            <artifactId>tika-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.tika</groupId>
            <artifactId>tika-parsers-standard-package</artifactId>
        </dependency>
        <!-- URL 抓取 -->
        <dependency>
            <groupId>org.jsoup</groupId>
            <artifactId>jsoup</artifactId>
        </dependency>
    </dependencies>
</project>
