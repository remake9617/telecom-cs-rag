# 前端环境变量模板（复制为 .env 后按需修改；.env 已 gitignore）
# 后端地址：dev 下作为 Vite 代理(/api)的转发目标；生产构建作为绝对 API 基址。禁止在代码中硬编码。
VITE_API_BASE_URL=http://localhost:8080
# 是否启用 MSW Mock：true=前端脱离后端独立演示；联调真实后端时置 false（dev 走 Vite 代理转发，无需后端 CORS）
# 提醒：.env 已被 gitignore，换机/全新克隆时此模板默认为 true 会静默进入 Mock 模式；联调前务必把 VITE_USE_MOCK 显式改为 false，改完需重启 dev server（构建期注入，热更新不生效）。
VITE_USE_MOCK=true
